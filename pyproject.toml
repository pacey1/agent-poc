[project]
name = "confluence-agent"
version = "0.1.0"
description = "Natural-language Confluence agent with Grok + MCP"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "httpx>=0.27.0",
  "pydantic>=2.8.0",
  "python-dotenv>=1.0.1",
  "pydantic-settings>=2.3.4",
  "langgraph>=0.2.50"
]

[tool.uv]
dev-dependencies = []
